#include<stdio.h>
int main(){
    // int a;
    // char b;
    // float c;
    // double d;
    // printf("size of integer data type=%d bytes",sizeof(a));
    // printf("\n size of character data type=%d bytes",sizeof(b));
    // printf("\n size of float data type=%d bytes",sizeof(c));
    // printf("\n size of double data type=%d bytes",sizeof(d));
    // return 0;
    // float num1,num2,product = 0;
    // printf("enter two floating point numbers :\n");
    // scanf("%f \n%f",&num1,&num2);
    // product = num1*num2;
    // printf("product of two floating point number = %2.f",product);
    int 51;
    printf("Enter the number: ");
    scanf("%d",&a);
    if(a<50)
    printf("a less then 50");
    else if (a<100);
    printf("a between 50&100");
    else
    printf("a is greater then 100");
    
    return 0;
}
